server {
    listen 80 default_server;
    listen [::]:80 default_server;

    root /var/www/html/tp-app;
    index index.html;

    server_name {{ ansible_host }};

    location / {
        try_files $uri $uri/ =404;
    }
}
